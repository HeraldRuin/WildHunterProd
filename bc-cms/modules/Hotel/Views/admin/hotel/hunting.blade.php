<div class="panel">
    <div class="panel-title"><strong>{{__('Maximum number of hunts for a base')}}</strong></div>
    <div class="panel-body">
        <div class="form-group">
            <label>{{__("How many hunts per day?")}}</label>
            <input type="text" value="{{$row->max_hunts_per_day}}" name="max_hunts_per_day" class="form-control">
        </div>
    </div>
</div>
